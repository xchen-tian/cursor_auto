# Normalize all text files to LF in the repo, checkout as-is
* text=auto eol=lf

# Ensure shell/PowerShell scripts keep correct endings
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
*.sh  text eol=lf
